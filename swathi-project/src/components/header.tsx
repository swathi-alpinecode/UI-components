import { ReactElement } from "react";
import { Title, Wrapper } from "./styledcomponent";

const Header = () => {
  return (
    <Wrapper>
      <Title>Header</Title>
    </Wrapper>
  );
};

export default Header;
