import { Title, Wrapper } from "@/styles/styledcomponent";
import { ReactElement } from "react";

const Header = () => {
  return (
    <Wrapper>
      <Title>Header</Title>
    </Wrapper>
  );
};

export default Header;
